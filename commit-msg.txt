fix: crash on Vercel - winston file transports on read-only filesystem

Winston was trying to create logs/error.log and logs/combined.log at
startup. Vercel's serverless filesystem is read-only so this threw
immediately before any request was handled.

Fix: only add file transports when NODE_ENV !== production.
Also simplify api/index.ts to remove dotenv (not needed on Vercel).
